import { Routes } from '@angular/router';
import { SelectionCellComponent } from './cell/cell';

export const routes: Routes = [
  {
    path: 'selection-cell',
    component: SelectionCellComponent,
  },
];
